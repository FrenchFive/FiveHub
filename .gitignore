# hub data (assets, database, reports) lives outside version control
/hub

# fonts are user-supplied — Fontshare's license forbids redistributing the
# files (see assets/fonts/README.md)
assets/fonts/*
!assets/fonts/README.md

# python
__pycache__/
*.pyc

# electron app
app/node_modules/
app/dist/

# generated on each machine by the installer / updater — never commit
app/package-lock.json
houdini/splash/*.png

# legacy locations
/test
/asset
/db
toolbar
